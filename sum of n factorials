def findFactSum(kk):
    f = 1
    Sum = 0
    for i in range(1, kk + 1):
        f = f * i
        Sum += f
    return Sum
if __name__ == "__main__":
    kk = int(input("enter the number"))
    print(findFactSum(kk))
